# Streamlit Community Cloud reads this file. It installs the project itself, which
# brings in the runtime dependencies declared in pyproject.toml -- one source of
# truth for versions rather than a second list that drifts from the first.
#
# This currently also installs dbt-core, dbt-duckdb, dlt and boto3, which the
# dashboard never imports. Measured, not assumed: importing all 18 modules the
# dashboard touches and then inspecting sys.modules shows dlt, dbt and boto3 are
# never reached. Moving them to an `etl` extra would cut the deployment's install
# time and footprint, and is the obvious follow-up -- deliberately not done in the
# same change as first getting the deployment working.
#
# Set the Python version to 3.11 in the Streamlit Cloud app settings.
# pyproject declares requires-python = ">=3.11,<3.12", and a mismatch fails the
# install with a message that does not mention Python at all.
.
